Projeto: Snake com pseudo-registradores (versão C++)

📌 CONCEITO BASE
-----------------
Este projeto é uma implementação do clássico "jogo da cobrinha" (Snake), utilizando conceitos inspirados no funcionamento interno de um CPU:

✅ Registradores simulados (limite de 40):
   - Representam variáveis de uso geral
   - Cada registrador é um bloco de 4 bytes (uint32_t)
   - Toda lógica do jogo utiliza esses registradores em vez de variáveis soltas

✅ Memória fixa:
   - Usamos um pool de memória alocado via `std::unique_ptr<uint8_t[]>`
   - Não há alocação dinâmica em tempo de execução (sem malloc/new)

✅ Gerenciamento manual:
   - Posicionamento da cobra, fruta, direção, etc. são armazenados diretamente nos registradores
   - O comportamento simula um sistema de troca/contexto similar ao que um processador faz com registradores e memória

---

🎮 FUNCIONALIDADES IMPLEMENTADAS
---------------------------------
1. Estrutura da memória/registradores:
   - Registradores: 40 posições de 4 bytes cada
   - Macros ou constantes definindo o uso de cada posição (ex: POS_X, POS_Y, LENGTH, etc.)

2. Sistema de renderização via buffer de tela:
   - Utiliza `WriteConsoleOutputCharacterA` para desenhar no console sem flicker
   - Tela limpa com bordas
   - Cobra e fruta desenhados corretamente
   - Posicionamento corrigido (sem "\n" no buffer)

3. Lógica de movimentação:
   - Cobra se move com as teclas WASD
   - Verificação de colisão com a parede
   - Fruta aparece aleatoriamente (com correção no uso de `rand()` e `srand()`)

4. Console ajustado automaticamente:
   - Redimensionamento de janela via WinAPI (`SetConsoleScreenBufferSize` + `SetConsoleWindowInfo`)

---

🛣️ PRÓXIMOS PASSOS SUGERIDOS
------------------------------
- Mostrar placar no topo da tela
- Aumentar velocidade conforme frutas comidas
- Tecla para pausar o jogo
- Movimento contínuo (sem precisar apertar tecla a cada frame)
- Reiniciar o jogo após Game Over
- Modo “hardcore” com dificuldade crescente

---
